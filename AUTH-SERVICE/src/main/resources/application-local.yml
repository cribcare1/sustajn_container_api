spring:
  application:
    name: AUTH-SERVICE

  datasource:
    url: jdbc:postgresql://ep-purple-salad-aoopfdy3-pooler.c-2.ap-southeast-1.aws.neon.tech/auth_service_3?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_e6xr3XVMcYFZ
    driver-class-name: org.postgresql.Driver
#    url: ${DB_URL}
#    password: ${DB_PASSWORD}

    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      idle-timeout: 30000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

server:
  port: 8081

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true

    service-url:
      defaultZone: http://localhost:8761/eureka/

  instance:
    prefer-ip-address: true

image:
  storage:
    root-path: C:/uploads