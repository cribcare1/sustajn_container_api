spring:
  application:
    name: ORDER-SERVICE

  datasource:
    url: jdbc:postgresql://ep-solitary-sunset-ao3lnirw-pooler.c-2.ap-southeast-1.aws.neon.tech/order_db_1?sslmode=require&channel_binding=require'
    username: neondb_owner
    password: npg_4cLW8brkUJvt
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      idle-timeout: 30000
      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

server:
  port: 8085

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

inventory:
  file:
    upload-dir: C:/InventoryUploads
